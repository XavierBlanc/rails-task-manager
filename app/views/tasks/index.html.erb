<h1> My tasks </h1>

<% @tasks.each do |task| %>
  <div>
    <%= link_to "#{task.title}", task_path("#{task.id}") %>
    <%= done = task.completed ? "[x]" : "[ ]" %>
  </div>
<% end %>

<%= link_to "add", new_task_path %>
